# Copy to a private .env file or export these before running saya against the
# example local Docker profiles in examples/connections.docker.toml.
# Development-only credentials for localhost — never use real secrets here.
SAYA_LOCAL_POSTGRES_PASSWORD=postgres
SAYA_LOCAL_MYSQL_PASSWORD=app
