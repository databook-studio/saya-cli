# Copy to a private file or export these values for local Docker testing.
# These match the development-only credentials in DataTalkie/docker/docker-compose.yml.
SAYA_DOCKER_POSTGRES_PASSWORD=databook
SAYA_DOCKER_MYSQL_PASSWORD=databook
