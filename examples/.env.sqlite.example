# Environment-only SQLite profile. read_only defaults to true; set false to allow the file to be opened writable (saya still enforces read-only SQL).
SAYA_PROFILE=env-only
SAYA_DB_TYPE=sqlite
SAYA_DB_PATH=./data/warehouse.sqlite3
SAYA_DB_READ_ONLY=true
